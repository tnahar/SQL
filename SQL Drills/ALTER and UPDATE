/* alter: when you can change the format of the table, such as adding, deleting and editing columns
update: when you change change the information in the data frame but cannot change the structure of the df */

/* 1. Change the name of the column from department_id to dept_id.*/

ALTER TABLE table_Name
RENAME COLUMN department_id TO dept_id;

/* 2. Add a column named annual_salary to the table.*/

ALTER TABLE table_Name
ADD annual_salary varchar(255)
